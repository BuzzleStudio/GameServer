{
  "name": "adminweb-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy — holds UGS service-account secrets server-side, forwards SPA requests to Unity Cloud Code.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.65.0"
  }
}
