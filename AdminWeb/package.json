{
  "name": "admin-web-mailbox",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "gen:ids": "node scripts/gen-payout-ids.mjs",
    "build": "npm run gen:ids && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.1.0",
    "vitest": "^1.4.0"
  }
}
